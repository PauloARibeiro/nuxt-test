{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.1.0",
    "prisma": "^4.9.0",
    "vitest": "^0.28.3"
  },
  "dependencies": {
    "@prisma/client": "^4.9.0"
  }
}
